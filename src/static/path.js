/* global require */
// TODO: PATH SHOULD BE ABSOLUTE OR DIRECTLY CDN URL
require.config({
    paths : {
        "fb"    : "${project.version}/js",
        "fb-core"  : "${project.version}"
    },
	waitSeconds: 0
});